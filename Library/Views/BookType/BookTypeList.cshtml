@model List<BookType>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3 class="text-center">Kiatap Tipi Listeleme Sayfası</h3>
<a asp-controller="BookType" asp-action="Create" class="btn btn-primary" style="float:right;">Yeni Ekle</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Aksiyonlar</th>

        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <th>@(i+1)</th>
                <td>@Model[i].Name</td>
                <td>
                 <a class="big-icon" asp-controller="BookType" asp-action="Edit" asp-route-id="@Model[i].ID"><i class="bi bi-pencil-square"></i></a>
                 <a class="big-icon margin-icon" asp-controller="BookType" asp-action="HardDelete"><i class="bi bi-x-circle"></i></a>
                </td>
            </tr>
        }
    </tbody>
</table>